/**
 * Drizzle schema — single source of truth for every table.
 * One file per domain (offerings, prospects, messages, …) re-exported here.
 * Populated in the database foundation unit; intentionally empty at scaffold.
 */

export {};
