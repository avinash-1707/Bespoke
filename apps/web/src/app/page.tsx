export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 p-6">
      <h1 className="text-xl font-semibold text-[var(--text-primary)]">
        Bespoke
      </h1>
      <p className="max-w-xs text-center text-sm text-[var(--text-secondary)]">
        AI-powered personalized outreach. Scaffold is live — features land
        unit by unit.
      </p>
    </main>
  );
}
