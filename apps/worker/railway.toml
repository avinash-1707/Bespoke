[build]
buildCommand = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @bespoke/worker build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
